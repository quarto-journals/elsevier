\documentclass[$for(classoption)$$classoption$$sep$,$endfor$]{elsarticle} %review=doublespace preprint=single 5p=2 column

$if(journal)$
  \journal{$journal$} % Sets Journal name
$endif$
\usepackage{lineno} % add
$if(number-lines)$
  \linenumbers % turns line numbering on
$endif$